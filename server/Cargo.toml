[package]
name = "mac-mgmt-server"
version = "0.1.0"
edition = "2024"

[features]
default = ["web", "server", "webui"]
web = ["webui", "dioxus/web"]
webui = ["dep:dioxus", "dep:wasm-bindgen", "dep:web-sys", "dep:dioxus-tabular", "dep:pulldown-cmark"]
server = ["webui", "dioxus/server", "dep:axum-oidc-client", "dep:axum-extra", "dep:rocket", "dep:sqlx", "dep:tokio", "dep:sha2", "dep:hex", "dep:rand", "dep:tracing-subscriber", "dep:anyhow", "dep:async-trait", "dep:toml", "dep:base64", "dep:reqwest", "dep:rustls", "dep:jsonschema", "dep:utoipa", "dep:utoipa-swagger-ui", "dep:schemars", "dep:similar", "dep:ed25519-dalek", "dep:pulldown-cmark", "dep:rust-embed", "dep:sentry", "dep:mac-mgmt-healer", "dep:tokio-stream", "mac-mgmt-common/observability"]
server-api-only = ["dep:rocket", "dep:sqlx", "dep:tokio", "dep:sha2", "dep:hex", "dep:rand", "dep:tracing-subscriber", "dep:anyhow", "dep:async-trait", "dep:toml", "dep:base64", "dep:reqwest", "dep:rustls", "dep:jsonschema", "dep:utoipa", "dep:utoipa-swagger-ui", "dep:schemars", "dep:ed25519-dalek", "dep:sentry", "dep:mac-mgmt-healer", "mac-mgmt-common/observability"]

[dependencies]
dioxus = { version = "0.7", features = ["fullstack", "router"], optional = true }
wasm-bindgen = { version = "=0.2.114", optional = true }
web-sys = { version = "0.3", features = ["EventSource", "MessageEvent", "Event"], optional = true }
rocket = { version = "0.5", default-features = false, features = ["json"], optional = true }
utoipa = { version = "5", features = ["rocket_extras", "uuid", "chrono"], optional = true }
utoipa-swagger-ui = { version = "9", features = ["rocket"], optional = true }
sqlx = { version = "0.8", features = ["runtime-tokio-rustls", "postgres", "uuid", "chrono", "json"], optional = true }
tokio = { version = "1", features = ["full"], optional = true }
uuid = { version = "1", features = ["v4", "serde", "js"] }
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
sha2 = { version = "0.10", optional = true }
hex = { version = "0.4", optional = true }
rand = { version = "0.9", optional = true }
clap = { version = "4", features = ["derive"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"], optional = true }
sentry = { workspace = true, features = ["tracing", "anyhow"], optional = true }
anyhow = { version = "1", optional = true }
async-trait = { version = "0.1", optional = true }
toml = { version = "0.8", optional = true }
axum-oidc-client = { version = "0.3", features = ["sql-cache-postgres", "redis"], optional = true }
jsonschema = { version = "0.45", default-features = false, features = ["resolve-http", "resolve-file", "tls-ring"], optional = true }
serde_json = "1"
base64 = { version = "0.22", optional = true }
axum-extra = { version = "0.10", features = ["cookie-private"], optional = true }
reqwest = { workspace = true, optional = true }
rustls = { workspace = true, optional = true }
dioxus-tabular = { version = "0.3", optional = true }
ed25519-dalek = { version = "2", optional = true }
mac-mgmt-common = { path = "../common" }
mac-mgmt-healer = { path = "../mac-mgmt-healer", optional = true }
tokio-stream = { version = "0.1", optional = true }
schemars = { version = "1.2.1", features = ["derive"], optional = true }
similar = { version = "2", optional = true }
pulldown-cmark = { version = "0.12", optional = true }
rust-embed = { version = "8", features = ["include-exclude"], optional = true }
