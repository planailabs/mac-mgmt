{
  "private": true,
  "scripts": {
    "tailwind": "tailwindcss -i input.css -o public/tailwind.css --watch",
    "tailwind:build": "tailwindcss -i input.css -o public/tailwind.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3"
  }
}
