# Skill

A packaged capability (instructions plus optional nix package and MCP server
dependencies) distributed to daemons. Skills are published in channels (e.g.
stable/beta); clusters attach specific skill channels, individually or via
bundles, and daemons sync the resolved store paths.
