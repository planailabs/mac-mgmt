# Instance

One managed machine, identified by its instance id (heartbeats key on it).
"Instance" and "node" are used interchangeably; the daemon is the process,
the instance is the machine it manages. The fleet dashboard lists instances
with health, versions, and services.
