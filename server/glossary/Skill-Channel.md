# Skill channel

A release track of a skill (for example stable or beta). Each channel carries
its own nix package list and MCP-server dependencies and resolves to concrete
store paths. Clusters attach channels, not skills directly.
