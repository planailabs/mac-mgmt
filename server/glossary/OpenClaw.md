# OpenClaw

OpenClaw is also an agent: an AI agent with a local gateway that runs on
managed instances. The daemon manages its configuration (the server ships an
OpenClaw config baseline), and its gateway exposes the locally available
models; skills and MCP servers assigned to the cluster become available to it.
