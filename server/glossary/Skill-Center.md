# Skill center

A remote catalog server that publishes skills, bundles, and MCP servers for
import. The server syncs registered skill centers periodically and offers
their entries as remote options when attaching to clusters.
