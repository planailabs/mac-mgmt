# Hermes

Hermes is an agent (the "Hermes agent"): an autonomous AI agent that runs on
managed machines. On this platform it is deployed to instances through the
skill system and uses the models and MCP servers assigned to the instance's
cluster.
