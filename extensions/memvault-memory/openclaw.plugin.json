{
  "id": "memvault-memory",
  "name": "Memvault Memory",
  "description": "P2P collaborative memory store for openclaw. Provides persistent, shared memory across devices and agents with knowledge graph, full-text search, and file attachments.",
  "kind": "memory",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "dataDir": {
        "type": "string",
        "description": "Path to memvault data directory",
        "default": "~/.local/share/memvault"
      },
      "autoCapture": {
        "type": "boolean",
        "description": "Automatically capture important information from conversations",
        "default": true
      },
      "autoRecall": {
        "type": "boolean",
        "description": "Automatically search memory before responding",
        "default": true
      },
      "maxRecallResults": {
        "type": "number",
        "description": "Maximum number of memory results to include in context",
        "default": 5
      },
      "defaultVisibility": {
        "type": "string",
        "enum": ["internal", "federated", "public"],
        "default": "internal"
      },
      "defaultTags": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Default tags to apply to all memories (scope:label format)",
        "default": ["agent:openclaw"]
      }
    }
  }
}
