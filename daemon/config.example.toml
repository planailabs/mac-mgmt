[metrics]
port = 9396

[openclaw]
provider = "ollama"
# Extra configuration merged into ~/.openclaw/openclaw.json
# extra_config = { "key" = "value" }

[ollama]
host = "127.0.0.1"
port = 11434
models = ["gemma3n", "qwen3.5"]
default_model = "gemma3n"
# Package flavour: cpu, rocm, cuda, vulkan
# "cpu" installs "ollama", others install "ollama-{flavour}"
flavour = "cpu"

# [server]
# url = "https://mgmt.example.com:8080"
# token = "customer-token-here"
