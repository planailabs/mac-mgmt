//! Tool-call validation layer — now provided by the generic `plan-ai-chat`
//! crate. Re-exported here so existing `crate::validation::*` paths keep
//! working.

pub use plan_ai_chat::validation::*;
